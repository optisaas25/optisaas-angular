import { PrismaClient } from " @prisma/client\; const prisma = new PrismaClient(); async function main() { const factures = await prisma.facture.groupBy({ by: [\type\, \statut\], _count: true, _sum: { totalTTC: true } }); console.log(\Facture Types Statuses:\, JSON.stringify(factures, null, 2)); const centers = await prisma.facture.groupBy({ by: [\centreId\], _count: true }); console.log(\Facture by Centre ID:\, JSON.stringify(centers, null, 2)); } main().catch(console.error).finally(() => prisma.());
