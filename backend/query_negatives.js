const { PrismaClient } = require(\" @prisma/client\\\); const prisma = new PrismaClient(); async function main() { const count = await prisma.factureFournisseur.count({ where: { montantTTC: { lt: 0 } } }); console.log(\\\Negative count:\\\, count); const sample = await prisma.factureFournisseur.findFirst({ where: { montantTTC: { lt: 0 } }, include: { echeances: true } }); console.log(\\\Sample:\\\, JSON.stringify(sample, null, 2)); process.exit(0); } main().catch(console.error);
